{
  "name": "skipfile",
  "version": "0.1.0",
  "description": "Append data, seek forward and seek backward inside a binary file.",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "author": "hij1nx",
  "license": "MIT",
  "dependencies": {
    "varint": "^3.0.0"
  },
  "devDependencies": {
    "async-benchmark": "^1.0.1",
    "benchmark": "^1.0.0",
    "rimraf": "^2.2.8",
    "tap": "^0.4.12"
  }
}
